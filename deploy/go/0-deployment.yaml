apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: go-app
    app.kubernetes.io/name: go-app
  name: go-app
  namespace: default
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/component: go-app
      app.kubernetes.io/name: go-app
  template:
    metadata:
      labels:
        app.kubernetes.io/component: go-app
        app.kubernetes.io/name: go-app
    spec:
      containers:
      - name: go-app
        image:  kemplerart/vex-svc:latest-test
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 8080
        - name: metrics
          containerPort: 8081
        resources:
          requests:
            memory: 2Gi
            cpu: 1500m
          limits:
            memory: 4Gi
            cpu: 2000m
        readinessProbe:
          httpGet:
            path: /healthz
            port: http
        livenessProbe:
          httpGet:
            path: /healthz
            port: http
