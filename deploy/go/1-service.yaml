apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: go-app
    app.kubernetes.io/name: go-app
  name: go-app
  namespace: default
spec:
  ports:
  - name: http
    port: 8080
    targetPort: http
  selector:
    app.kubernetes.io/component: go-app
    app.kubernetes.io/name: go-app
  type: ClusterIP
